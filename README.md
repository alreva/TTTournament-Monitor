# TTTournament Monitor

Background worker that monitors [kozts.pl](https://kozts.pl/) for new table tennis tournament announcements and prepares registration tasks.

```
# build
cd TTTournament.Monitor
 dotnet build -c Release
```
